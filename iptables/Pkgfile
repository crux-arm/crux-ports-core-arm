# Description: A Linux kernel packet filter control tool
# URL: https://www.iptables.org/
# Maintainer: CRUX System Team, core-ports at crux dot nu
# Maintainer: CRUX-ARM System Team, devel at crux-arm dot nu
# Depends on:

name=iptables
version=1.6.0
release=1
source=(https://www.netfilter.org/pub/iptables/$name-$version.tar.bz2) 

build () {
  cd $name-$version

  ./configure \
    --prefix=/usr \
    --libexecdir=/usr/lib \
    --enable-libipq \
    --disable-nftables

  make
  make DESTDIR=$PKG install
}
